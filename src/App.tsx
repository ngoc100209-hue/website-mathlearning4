// Note: This file is legacy. Next.js routing is handled by the 'app/' directory structure.
// Keep this for reference, but the actual routing is now in app/layout.tsx and app pages.

export default function App() {
  return (
    <div className="flex min-h-screen flex-col font-body text-on-surface selection:bg-primary-fixed">
      {/* Routing is now handled by Next.js app directory */}
    </div>
  );
}
